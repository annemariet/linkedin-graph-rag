# LinkedIn API Client Environment Configuration
LINKEDIN_ACCOUNT=your-linkedin-account

# Browser Use (autonomous browser agent) — pick one LLM provider:
# Mammouth (OpenAI-compatible gateway, access to all major models)
# MAMMOUTH_API_KEY=your-mammouth-api-key
# Anthropic direct
# ANTHROPIC_API_KEY=your-anthropic-api-key
# Ollama: no key needed, just run `ollama serve`
