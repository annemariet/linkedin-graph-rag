{
  "name": "linkedin-api-client",
  "type": "python",
  "description": "Official Python client library for LinkedIn APIs",
  "services": [],
  "ports": [8002, 8003],
  "scripts": {
    "dev": "uv run flask --app examples/flask_app.py run --host 0.0.0.0 --port 8002",
    "test": "uv run pytest",
    "test-cov": "uv run pytest --cov=linkedin_api --cov-report=term-missing",
    "lint": "uv run flake8 linkedin_api tests examples",
    "format": "uv run black linkedin_api tests examples",
    "format-check": "uv run black --check linkedin_api tests examples",
    "build": "uv build",
    "install": "uv sync",
    "install-dev": "uv sync --all-extras"
  },
  "environment": {
    "PYTHONPATH": ".",
    "PROJECT_ROOT": "/workspaces/amai-lab/projects/linkedin"
  },
  "dependencies": {
    "python": ">=3.7",
    "system": ["git"],
    "package_manager": "uv"
  }
}